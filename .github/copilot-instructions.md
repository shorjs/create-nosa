# Copilot Instructions

This repository keeps package manifests reproducible.

When reviewing changes to any `package.json`, require every dependency and devDependency to use an exact stable version. Flag semver ranges, `latest`, and prerelease tags unless the change explicitly explains why an exception is required.
